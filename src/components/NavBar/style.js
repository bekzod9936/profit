import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  justify-content: space-around;
  width: 50%;
  flex-wrap: wrap;
  a {
    color: white;
    cursor: pointer;
  }
`
